# 与 pyproject.toml [project.dependencies] 保持一致；安装：pip install -r requirements.txt
# 推荐开发安装：pip install -e .

nonebot2[fastapi,websockets]>=2.2.0
nonebot-adapter-onebot>=2.4.0
python-dotenv>=1.0.0
httpx>=0.27.0
openai>=1.30.0
pillow>=10.0.0
